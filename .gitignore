# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
# DebugLog panel exports (per-session, never tracked).
sc-debug-*.txt
# Local debug-log scratch directory (DebugLog downloads + recording
# gaps.json sidecars users drop here for sharing in issues).
dbg-logs/
# Scratch image used to share screenshots in chat.
tmp.png

node_modules
dist
dist-ssr
*.local

# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
.pnp.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/tmp/
/.agents/skills/tauri-v2/references/capabilities-reference.md
/.agents/skills/tauri-v2/references/ipc-patterns.md
/.agents/skills/tauri-v2/SKILL.md

# Rust build output
/target/
**/target/

# Stale TypeScript incremental-build artefacts.
*.tsbuildinfo

# SuperDirt runtime deps (Dirt-Samples / Vowel / sc3-plugins) fetched
# by `yarn superdirt-setup`. ~50+ MB; never tracked.
/superdirt-deps/

# Stray sclang/scsynth log captures from local debugging — not source.
/errors.log
/scsynth.log
/sclang.log
