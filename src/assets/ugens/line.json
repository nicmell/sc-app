[{
    "name": "A2K", "rates": ["kr"], "defaults": [["in", 0]], "doc": "audio rate to control rate converter via linear interpolation", "argDocs": {
      "in": "input signal"
    }
  },  {
    "name": "AmpComp", "rates": ["ir", "ar", "kr"], "defaults": [["freq", 261.6256],  ["root", 261.6256],  ["exp", 0.3333]], "summary": "Basic psychoacoustic amplitude compensation.\" , :rates #{:ir :ar :kr} :check (when-ar (first-input-ar \"freq must be audio rate\")) :doc \"amplitude compensation: because higher frequencies are normally perceived as louder. Note that for frequencies very much smaller than root the amplitudes can become very high. In this case limit the freqor use amp-comp-a Implements the (optimized) formula: compensationFactor = (root / freq) ** exp", "doc": "amplitude compensation: because higher frequencies are normally perceived as louder. Note that for frequencies very much smaller than root the amplitudes can become very high. In this case limit the freqor use amp-comp-a Implements the (optimized) formula: compensationFactor = (root / freq) ** exp", "argDocs": {
      "freq": "Input frequency value. For freq == root, the output is 1.0.", "root": "Root freq relative to which the curve is calculated (usually lowest freq)", "exp": "Exponent: how steep the curve decreases for increasing freq"
    }
  },  {
    "name": "AmpCompA", "extends": "AmpComp", "rates": ["ir", "ar", "kr"], "defaults": [["freq", 1000],  ["root", 0],  ["minAmp", 0.32],  ["rootAmp", 1]], "summary": "Basic psychoacoustic amplitude compensation (ANSI A-weighting curve).", "doc": "Higher frequencies are normally perceived as louder, which amp-comp-a compensates. Following the measurings by Fletcher and Munson, the ANSI standard describes a function for loudness vs. frequency. Note that this curve is only valid for standardized amplitude. 1 For a simpler but more flexible curve, see amp-comp", "argDocs": {
      "freq": "Input frequency value. For freq == root, the output is root-amp", "root": "Root freq relative to which the curve is calculated (usually lowest freq)", "minAmp": "Amplitude at the minimum point of the curve (around 2512 Hz)", "rootAmp": "Amplitude at the root frequency."
    }
  },  {
    "name": "DC", "rates": ["ar", "kr"], "defaults": [["in", null]], "doc": "outputs the initial value you give it.", "argDocs": {
      "in": "constant value to output, cannot be modulated, set at initialisation time"
    }
  },  {
    "name": "K2A", "rates": ["ar"], "defaults": [["in", 0]], "doc": "control rate to audio rate converter via linear interpolation.", "argDocs": {
      "in": "input signal"
    }
  },  {
    "name": "Line", "rates": ["ar", "kr"], "defaults": [["start", 0],  ["end", 1],  ["dur", 1],  ["action", 0]], "summary": "Line generator.", "doc": "Generates a line from the start value to the end value.", "argDocs": {
      "start": "Starting value", "end": "Ending value", "dur": "Duration in seconds", "action": "A done action to be evaluated when the line is completed. Default: NO-ACTION"
    }
  },  {
    "name": "LinExp", "rates": ["ar", "kr"], "defaults": [["in", 0],  ["srclo", 0],  ["srchi", 1],  ["dstlo", 1],  ["dsthi", 2]], "summary": "Map a linear range to an exponential range", "doc": "Convert from a linear range to an exponential range. The dstlo and dsthi arguments must be nonzero and have the same sign.", "argDocs": {
      "in": "Input to convert", "srclo": "Lower limit of input range", "srchi": "Upper limit of input range", "dstlo": "Lower limit of output range", "dsthi": "Upper limit of output range"
    }
  },  {
    "name": "Silent", "rates": ["ar"], "defaults": [["numChannels", 1]], "doc": "Continuously outputs 0", "argDocs": {
      "numChannels": "Number of channels of silence."
    }
  },  {
    "name": "T2A", "rates": ["ar"], "defaults": [["in", 0],  ["offset", 0]], "doc": "control rate trigger to audio rate trigger converter (maximally one per control period).", "argDocs": {
      "in": "input signal", "offset": "sample offset within control period"
    }
  },  {
    "name": "T2K", "extends": "A2K", "rates": ["kr"], "defaults": [["in", 0]], "doc": "audio rate trigger to control rate trigger converter. Uses the maxiumum trigger in the input during each control period.", "argDocs": {
      "in": "input signal"
    }
  },  {
    "name": "XLine", "rates": ["ar", "kr"], "defaults": [["start", 1],  ["end", 2],  ["dur", 1],  ["action", 0]], "summary": "Exponential line generator.", "doc": "Generates an exponential curve from the start value to the end value. Both the start and end values must be non-zero and have the same sign.", "argDocs": {
      "start": "Starting value", "end": "Ending value", "dur": "Duration in seconds", "action": "A done action to be evaluated when the line is completed. Default: NO-ACTION"
    }
  }
]
